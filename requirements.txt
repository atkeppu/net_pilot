# This project has no external Python package dependencies for runtime.
# All core functionality relies on the Python standard library and Windows system commands.
# Development and build dependencies are listed in requirements-dev.txt.